pre-commit:
  parallel: true
  jobs:
    - name: typecheck
      run: npm run typecheck

    - name: lint
      run: npm run lint

pre-push:
  parallel: true
  jobs:
    - name: test
      run: npm run test
